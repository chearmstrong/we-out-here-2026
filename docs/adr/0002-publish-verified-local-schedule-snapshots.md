# Publish verified local schedule snapshots

**Status:** accepted

The planner will ship manually verified, versioned local Schedule Snapshots from the repository rather than scrape or fetch official set times at runtime. Each snapshot displays its last-checked date; connected users are offered an update while their existing cached application and itinerary remain usable offline. This favours predictable festival-day reliability and reviewable data updates over automatic synchronisation.
